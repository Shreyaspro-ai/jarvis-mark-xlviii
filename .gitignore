# Secrets — never commit
config/api_keys.json
config/certs/
*.key
*.pem
*.crt

# Personal / runtime data
memory/long_term.json
*.log

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Backups / editor junk
*.bak
*.bak-*
.DS_Store

# self-improving learning loop (personal, never upload)
memory/skills.json
memory/history.db

# web tooling (installed by install.sh, not committed)
tools/.venv-web/
tools/node_modules/
tools/package-lock.json
memory/web/

# WhatsApp Web login session - NEVER commit
memory/whatsapp-profile/
